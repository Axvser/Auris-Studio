<UserControl x:Class="Auris_Studio.Views.NoteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Auris_Studio.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:vm="clr-namespace:Auris_Studio.ViewModels.MidiEvents"
             d:DataContext="{d:DesignInstance Type=vm:NoteEventViewModel,IsDesignTimeCreatable=True}"
             Focusable="True"
             BorderBrush="Black"
             BorderThickness="1">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="8*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Border x:Name="LeftArea"
                Grid.Column="0"
                Cursor="SizeWE"
                Background="#01FFFFFF"
                MouseDown="LeftArea_MouseDown">
            
        </Border>
        <Border x:Name="CenterArea"
                Grid.Column="1"
                Cursor="ScrollAll"
                Background="#01FFFFFF"
                MouseDown="CenterArea_MouseDown">

        </Border>
        <Border x:Name="RightArea"
                Grid.Column="2"
                Cursor="SizeWE"
                Background="#01FFFFFF"
                MouseDown="RightArea_MouseDown">

        </Border>
    </Grid>
</UserControl>
