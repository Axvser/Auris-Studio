<UserControl x:Class="Auris_Studio.Views.PianoKeyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Auris_Studio.Views"
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="60"
             xmlns:vm="clr-namespace:Auris_Studio.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:PianoKeyViewModel,IsDesignTimeCreatable=True}"
             BorderBrush="Black">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="0.618*"/>
        </Grid.ColumnDefinitions>
        <Viewbox Grid.Column="1">
            <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType=local:PianoKeyView},Path=Text}" 
                       TextAlignment="Center"
                       Foreground="Black"/>
        </Viewbox>
    </Grid>
</UserControl>
