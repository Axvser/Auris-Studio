<UserControl x:Class="Auris_Studio.Views.Button"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Auris_Studio.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#01FFFFFF">
    <UserControl.Template>
        <ControlTemplate>
            <Border ClipToBounds="True"
                    Width="{TemplateBinding Width}"
                    Height="{TemplateBinding Height}"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="{Binding RelativeSource={RelativeSource AncestorType=local:Button},Path=CornerRadius}"
                    MouseDown="Border_MouseDown"
                    MouseUp="Border_MouseUp"
                    MouseEnter="Border_MouseEnter"
                    MouseLeave="Border_MouseLeave">
                <Grid>
                    <Border Name="Hover"
                            ClipToBounds="True"
                            CornerRadius="{Binding RelativeSource={RelativeSource AncestorType=local:Button},Path=CornerRadius}">
                        <Border.Effect>
                            <BlurEffect Radius="{Binding RelativeSource={RelativeSource AncestorType=local:Button},Path=BlurRadius}"/>
                        </Border.Effect>
                    </Border>
                    <Viewbox x:Name="Icon" Margin="{Binding RelativeSource={RelativeSource AncestorType=local:Button},Path=ContentMargin}"/>
                </Grid>
            </Border>
        </ControlTemplate>
    </UserControl.Template>
</UserControl>
